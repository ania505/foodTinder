const Sequelize = require('sequelize');
const db = require('../db');

// const Melody = db.define('melody', {
//     melody: {

//     }
// })

//not sure what to do with this db until i play around with the new tech

// module.exports = Melody;